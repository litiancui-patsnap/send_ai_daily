# AI 日报自动化系统

每个工作日自动从 RSS 源抓取 AI 领域最新动态，使用大模型筛选 Top 3 内容并生成日报，推送到飞书群。

---

## 功能特性

- ⏰ **定时执行**: 周一到周五北京时间 10:07（GitHub Actions cron）
- 📰 **RSS 抓取**: 从 19 个高质量源抓取最近 48 小时内容
- 🤖 **智能筛选**: 使用大模型 API 对内容打分（0~10），选择 Top 3
- 🔧 **多模型支持**: 支持 OpenAI / 通义千问（Qwen）
- 📊 **角色化日报**: 针对老板、产品、工程师等 10 个角色输出影响分析
- 🔔 **飞书推送**: 发送 interactive card，支持签名校验
- 🔄 **去重机制**: 基于 sha256(link) 去重，持久化到 `data/sent_hashes.txt`

---

## 快速开始

### 1. Fork 本仓库

Fork 到你的 GitHub 账号下。

### 2. 配置 Secrets

进入仓库 **Settings > Secrets and variables > Actions > New repository secret**，添加以下 Secrets：

#### 方案 A：使用 OpenAI

| Secret 名称 | 说明 | 示例 |
|-------------|------|------|
| `LLM_PROVIDER` | 模型提供商 | `openai` |
| `OPENAI_API_KEY` | OpenAI API Key | `sk-...` |
| `FEISHU_WEBHOOK_URL` | 飞书自定义机器人 Webhook URL | `https://open.feishu.cn/open-apis/bot/v2/hook/...` |
| `FEISHU_SECRET` | 飞书机器人签名密钥（可选） | `abc123...` |
| `RSS_URLS` | RSS 源列表（多行文本，每行一个 URL） | 见下方示例 |

#### 方案 B：使用通义千问（推荐国内用户）

| Secret 名称 | 说明 | 示例 |
|-------------|------|------|
| `LLM_PROVIDER` | 模型提供商 | `qwen` |
| `DASHSCOPE_API_KEY` | 阿里云 DashScope API Key | `sk-...` |
| `QWEN_MODEL` | 通义千问模型名称（可选，默认 qwen-plus） | `qwen-plus` 或 `qwen-max` |
| `FEISHU_WEBHOOK_URL` | 飞书自定义机器人 Webhook URL | `https://open.feishu.cn/open-apis/bot/v2/hook/...` |
| `FEISHU_SECRET` | 飞书机器人签名密钥（可选） | `abc123...` |
| `RSS_URLS` | RSS 源列表（多行文本，每行一个 URL） | 见下方示例 |

#### RSS_URLS 示例

```
https://openai.com/blog/rss.xml
https://www.anthropic.com/news/rss.xml
https://ai.googleblog.com/feeds/posts/default
https://blog.langchain.dev/rss/
https://huggingface.co/blog/feed.xml
```

### 3. 飞书机器人配置

#### 创建自定义机器人

1. 在飞书群聊中，点击 **设置 > 群机器人 > 添加机器人 > 自定义机器人**
2. 设置机器人名称和描述
3. **安全设置 > 签名校验**（可选）：
   - 勾选"启用签名校验"
   - 复制 `Secret` 到 GitHub Secrets 的 `FEISHU_SECRET`
4. 复制 `Webhook 地址` 到 GitHub Secrets 的 `FEISHU_WEBHOOK_URL`

#### 签名校验说明

如果启用签名校验，代码会自动生成签名：

```
sign = base64(hmac_sha256(timestamp + "\n" + secret))
```

如果不启用，`FEISHU_SECRET` 可留空。

### 4. 手动触发测试

进入 **Actions > AI Daily Report > Run workflow**，手动触发一次测试。

---

## 本地测试

### 安装依赖

```bash
pip install -r requirements.txt
```

### 设置环境变量

#### 使用 OpenAI

```bash
export LLM_PROVIDER="openai"
export OPENAI_API_KEY="sk-..."
export FEISHU_WEBHOOK_URL="https://open.feishu.cn/open-apis/bot/v2/hook/..."
export FEISHU_SECRET="abc123..."
export RSS_URLS="https://openai.com/blog/rss.xml
https://blog.langchain.com/rss"
```

#### 使用通义千问

```bash
export LLM_PROVIDER="qwen"
export DASHSCOPE_API_KEY="sk-..."
export QWEN_MODEL="qwen-plus"
export FEISHU_WEBHOOK_URL="https://open.feishu.cn/open-apis/bot/v2/hook/..."
export FEISHU_SECRET="abc123..."
export RSS_URLS="https://openai.com/blog/rss.xml
https://blog.langchain.com/rss"
```

### 运行脚本

```bash
python send_ai_daily.py
```

---

## 工作流程

```
┌─────────────────────┐
│  GitHub Actions     │
│  Cron: 0 2 * * 1-5  │ (UTC 02:00 = 北京 10:00)
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  1. 抓取 RSS        │ (最近 48h, 最多 30 条)
│  2. 去重 (sha256)   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  3. LLM 评分        │ (0~10 分)
│  4. 选择 Top 3      │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  5. 生成日报        │ (JSON 结构)
│  6. 飞书推送        │ (Interactive Card)
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  7. 更新去重文件    │ (data/sent_hashes.txt)
│  8. Git commit      │ (chore: update sent hashes)
└─────────────────────┘
```

---

## 日报输出示例

### 飞书消息卡片结构

- **标题**: 📰 AI 日报 | 2025-12-26
- **技术变化**: 2~3 条要点
- **角色影响**: 10 个角色逐行展示
- **建议动作**: 🧪试点 / 👀观察 / ❌忽略
- **来源**: 可点击链接

---

## 文件说明

```
send_ai_daily/
├── .github/workflows/ai-daily.yml  # GitHub Actions 工作流
├── data/
│   ├── .gitkeep                    # 占位文件
│   └── sent_hashes.txt             # 去重记录（自动生成）
├── requirements.txt                # Python 依赖
├── send_ai_daily.py                # 主脚本
└── README.md                       # 本文档
```

---

## 常见问题

### Q1: 为什么没有推送？

1. 检查 Secrets 是否配置正确
2. 查看 Actions 日志，确认是否有新内容
3. 确认 RSS 源是否可访问

### Q2: 飞书推送失败？

1. 检查 `FEISHU_WEBHOOK_URL` 是否正确
2. 如果启用签名校验，确认 `FEISHU_SECRET` 正确
3. 查看飞书机器人是否被移除或禁用

### Q3: 如何调整评分标准？

修改 [send_ai_daily.py](send_ai_daily.py#L150) 中的 `system_prompt`。

### Q4: 如何调整 Top N 数量？

修改 [send_ai_daily.py](send_ai_daily.py#L30) 中的 `TOP_N = 3`。

---

## License

MIT
